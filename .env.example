# ShelfAI Backend Environment Variables
HOST=0.0.0.0
PORT=8000
LOG_LEVEL=info

# CORS — set to your frontend URL in production
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# Storage paths (created automatically if missing)
UPLOAD_DIR=uploads
OUTPUT_DIR=outputs
